<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:input="http://xamarin.com/schemas/2014/forms/design"
             x:Class="ShopingList.GroceriesPage"
             Title="Add Product">

    <!-- DODAWANIE NOWEJ NOTKI - (WEJSCIE W NOTE ENTRY PAGE PO KLIKNIECU  "OnNoteAddedClicked")-->
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="+"
                     Clicked="OnNoteAddedClicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <ListView x:Name="listView"
                  ItemSelected="OnListViewItemSelected"
                  HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>

                        <StackLayout Orientation="Horizontal" Spacing="0">
                            <Label x:Name="textLabel" Text="{Binding Text}">

                            </Label>
                            <Label x:Name="numberLabel" Text="{Binding Number}" IsVisible="true">

                            </Label>
                            <input:CheckBox x:Name="checkBox"  HorizontalOptions="EndAndExpand" CheckedChanged="checkBox_CheckedChanged"/>
                        </StackLayout>

                    </ViewCell>
                </DataTemplate>

            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>